// HyperText removed — no longer used in the new light-mode aesthetic.
// This file is kept to avoid broken imports if any stale references exist.
export default function HyperText({ text }) {
  return <span>{text}</span>;
}
